import { CSSObject } from "@emotion/react";

export const readMoreHoverStyles: CSSObject = {
    border: '1px solid blue',
    color: 'blue'
}

export const deleteHoverStyles: CSSObject = {
    opacity: '0.6',
    border: '2px solid rgba(255, 255, 255, 0)',
    borderRadius: '5px',
    cursor: 'pointer'
}